{
  "name": "@gertjanreynaert/design-tokens-experiment",
  "version": "1.0.0",
  "description": "experiment for using npm hosted design tokens in an internal project",
  "main": "build.js",
  "scripts": {
    "build": "node_modules/.bin/style-dictionary build",
    "clean": "rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gertjan Reynaert",
  "license": "Apache-2.0",
  "devDependencies": {
    "style-dictionary": "2.9.0"
  },
  "files": [
    "build/*"
  ]
}
